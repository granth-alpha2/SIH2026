import UnavailableFeature from "../components/UnavailableFeature";

export default function AssistantPage() {
  return <UnavailableFeature eyebrow="ASSISTANT" title="Crop assistant" description="The assistant will be available after authenticated farm context and an LLM provider are configured." />;
}